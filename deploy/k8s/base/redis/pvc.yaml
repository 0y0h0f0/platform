apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-data
  namespace: task-platform
  labels:
    app: redis
    app.kubernetes.io/part-of: task-platform
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
