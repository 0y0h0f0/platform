apiVersion: v1
kind: Service
metadata:
  name: alertmanager
  namespace: task-platform
  labels:
    app: alertmanager
    app.kubernetes.io/part-of: task-platform
spec:
  type: ClusterIP
  ports:
    - port: 9093
      targetPort: 9093
      protocol: TCP
      name: http
  selector:
    app: alertmanager
