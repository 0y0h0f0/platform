apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-data
  namespace: task-platform
  labels:
    app: grafana
    app.kubernetes.io/part-of: task-platform
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
