apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: task-platform
  labels:
    app: web
    app.kubernetes.io/part-of: task-platform
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: web
