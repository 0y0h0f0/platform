apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: loki-data
  namespace: task-platform
  labels:
    app: loki
    app.kubernetes.io/part-of: task-platform
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
