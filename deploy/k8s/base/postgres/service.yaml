apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: task-platform
  labels:
    app: postgres
    app.kubernetes.io/part-of: task-platform
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    app: postgres
